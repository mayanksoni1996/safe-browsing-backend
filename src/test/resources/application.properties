# Test configuration
spring.application.name=safe-browsing-test
spring.threads.virtual.enabled=true
# MongoDB configuration is handled by Testcontainers
# MinIO configuration is handled by Testcontainers
# Safe Browsing configuration
safe-browsing.minio-bucket-name=safe-browsing-test
# Disable scheduled tasks for tests
spring.task.scheduling.enabled=false